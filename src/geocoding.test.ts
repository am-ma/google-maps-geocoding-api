import { getGeocode } from "./geocoding";

describe('Geocoding', () => {
  describe('getGeocode', () => {
    it('should return', () => {
      getGeocode();
      expect(true).toBeTruthy();
    });
  });
});